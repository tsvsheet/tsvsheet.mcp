// Package tsvsheetmcp is the module root for tsvsheet-mcp — the Model Context
// Protocol (MCP) server that exposes the tsvsheet engine as stdio tools so AI
// agents can compute and inspect .tsvt spreadsheets. The runnable entry point
// is cmd/tsvsheet-mcp; the server, its tool handlers, and the grid formatters
// live under internal/app, and the engine itself is github.com/tsvsheet/go-tsvsheet.
package tsvsheetmcp
